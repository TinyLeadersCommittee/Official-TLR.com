---
interface Props {
  prevUrl?: string;
  prevTitle?: string;
  nextUrl?: string;
  nextTitle?: string;
}

const { prevUrl, prevTitle, nextUrl, nextTitle } = Astro.props;
---

<nav class="series-nav">
  
  {/* PREVIOUS BUTTON */}
  {prevUrl ? (
    <a href={prevUrl} class="nav-card prev">
      <span class="nav-label">Previous</span>
      <div class="nav-inner">
        <span class="nav-arrow">&larr;</span>
        <span class="chapter-title">{prevTitle}</span>
      </div>
    </a>
  ) : (
    <div class="nav-spacer"></div>
  )}

  {/* NEXT BUTTON */}
  {nextUrl ? (
    <a href={nextUrl} class="nav-card next">
      <span class="nav-label">Next</span>
      <div class="nav-inner">
        <span class="chapter-title">{nextTitle}</span>
        <span class="nav-arrow">&rarr;</span>
      </div>
    </a>
  ) : (
    <div class="nav-spacer"></div>
  )}

</nav>

<style>
  /* --- CONTAINER --- */
  .series-nav {
    margin-top: 6rem;
    padding-top: 2rem;
    border-top: 1px solid var(--border-subtle);
    display: flex;
    justify-content: space-between;
    align-items: flex-end; /* Aligns to bottom on desktop */
    gap: 1rem;
  }

  /* --- CARD SHARED STYLES --- */
  .nav-card {
    display: flex;
    flex-direction: column;
    text-decoration: none;
    width: 100%;
    max-width: 300px;
    padding: 1rem;
    border-radius: 8px;
    border: 1px solid transparent;
    transition: all 0.2s ease;
  }

  .nav-card:hover {
    background-color: var(--surface);
    border-color: var(--border-subtle);
    transform: translateY(-2px);
  }

  /* --- INTERNAL ELEMENTS --- */
  .nav-inner {
    display: flex;
    align-items: center;
    gap: 0.75rem;
  }

  .nav-label {
    font-size: 0.75rem;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    color: var(--text-muted);
    margin-bottom: 0.5rem;
    font-weight: bold;
  }

  .chapter-title {
    font-size: 1rem;
    font-weight: bold;
    color: var(--heading-color);
  }

  .nav-arrow {
    font-size: 1.25rem;
    color: var(--accent);
  }

  /* --- DESKTOP ALIGNMENT --- */
  .prev {
    align-items: flex-start;
    text-align: left;
  }

  .next {
    align-items: flex-end;
    text-align: right;
  }
  
  /* Preserves flex alignment for orphaned buttons */
  .nav-spacer {
    flex: 1; 
  }

  /* --- MOBILE RESPONSIVE --- */
  @media (max-width: 600px) {
    .series-nav {
      /* Reverses stack order for mobile hierarchy */
      flex-direction: column-reverse; 
      gap: 1.5rem;
      align-items: stretch; /* Forces full width */
    }
    
    .nav-card {
      max-width: 100%;
      /* Centers text alignment */
      align-items: center;
      text-align: center;
      
      /* Applies mobile button background styling */
      background-color: var(--surface);
      border-color: var(--border-subtle);
    }
    
    /* Centers internal flex items */
    .prev .nav-inner, .next .nav-inner {
        justify-content: center;
    }
    
    /* Hides empty spacers on mobile */
    .nav-spacer { display: none; } 
  }
</style>